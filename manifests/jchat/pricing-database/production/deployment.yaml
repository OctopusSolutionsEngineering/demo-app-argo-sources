apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-database-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: pricing-database-service
  template:
    metadata:
      labels:
        component: pricing-database-service
    spec:
      containers:
        - name: pricing-database
          image: index.docker.io/octopussolutionsengineering/ghu-instant-pay-gateway:26.03.18222046
          ports:
            - name: http-route
              containerPort: 80
          env:
            - name: DATABASE_SERVER
              value: "production"
            - name: DATABASE_NAME
              value: "pricing-database"
